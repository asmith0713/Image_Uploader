<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Buddy Image | Upload Images Anywhere</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
  <style>
    body {
      margin: 0;
      padding: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
      background: url("2.jpg") no-repeat center center/cover;
    }

    .container {
      background-color: rgba(255, 255, 255, 0.7); /* Adjust the alpha (fourth) value for transparency */
      padding: 20px;
      border-radius: 10px;
    }

    .card {
      background-color: rgba(255, 255, 255, 0.7); /* Adjust the alpha (fourth) value for transparency */
      border-radius: 10px;
    }
  </style>
</head>
<body>
  <div class="container mt-5">
    <div class="card">
      <div class="card-body">
        <h5 class="card-title">Welcome</h5>
        <form id="uploadForm" action="/upload" method="post" enctype="multipart/form-data">
          <div class="mb-3">
            <label for="fileInput" class="form-label">Upload your Photos</label>
            <input type="file" class="form-control" id="file" name="files" accept=".jpg, .jpeg, .png" multiple required>
          </div>
          <button type="submit" class="btn btn-primary">Upload</button>
        </form>
        <div class="mt-3" id="uploadResult"></div>
      </div>
    </div>
  </div>
</body>
</html>
